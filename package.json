{
  "name": "carpen",
  "version": "1.0.0",
  "main": "lib/index.js",
  "scripts": {
    "start": "tsc --watch",
    "clean": "rm -rf lib",
    "build": "npm run clean && tsc",
    "prepublish": "npm run build"
  },
  "files": [
    "lib"
  ],
  "dependencies": {
    "carlo": "^0.9.46",
    "parcel-bundler": "^1.12.4"
  },
  "devDependencies": {
    "typescript": "^3.6.4"
  }
}
